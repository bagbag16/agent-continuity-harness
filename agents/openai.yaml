interface:
  display_name: "ACH"
  short_description: "Continuity harness for long-running AI agent work"
  default_prompt: "Use $ach to keep this long-running task stable. Start in guard-mode, and enter continuity-mode only if handoff, recovery, formal state, or cross-window continuation is needed."
